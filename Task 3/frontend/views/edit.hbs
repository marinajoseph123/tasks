{{>header}}
{{>navbar}}
<section class="container my-5">
    <form  method="post"> 
        <div class="mb-3">
            <input name="id" disabled placeholder="please enter your name" class="form-control" value="{{user.id}}">
        </div>
        <div class="mb-3">
            <input name="name" placeholder="please enter your name" class="form-control" value="{{user.name}}">
        </div>
        <div class="mb-3">
            <input name="title" placeholder="please enter your title" class="form-control"  value="{{user.title}}">
        </div>
        <div class="mb-3">
            <input name="content" placeholder="please enter content"  class="form-control"  value="{{user.content}}">
        </div>
         <div class="mb-3">
            <input name="active" type="checkbox" value="{{user.status}}">status
        </div>
        <div class="mb-3">
            <button class="btn btn-primary">Edit User</button>
        </div>
    </form>
</section>

{{>footer}}
    